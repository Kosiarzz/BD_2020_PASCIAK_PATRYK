create or replace FUNCTION LOGOWANIE  --tworzenie funkcji o nazwwie FUNCTION1
---MIEJSCE NA ZMIENNE WEJŚCIOWE
(znak in VARCHAR2,
znak2 in VARCHAR2)
RETURN VARCHAR2 --co zwraca funkcja (typ danych)
IS
--zmienne lokalne funkcji
ilosc NUMBER;
BEGIN
--co chcemy aby funkcja wykonwywala
IF (length(znak)>0 and length(znak2)>0) then
  RETURN 'TRUE'; --zwracana wartosc przez funkcje
ELSE
  RETURN 'FALSE'; --zwracana wartość przez funkcje
  END IF;
END LOGOWANIE; --koniec funkcji


create or replace FUNCTION PESEL  --tworzenie funkcji o nazwwie FUNCTION1
---MIEJSCE NA ZMIENNE WEJŚCIOWE
(pesel in VARCHAR2,
telefon in VARCHAR2
)
RETURN VARCHAR2 --co zwraca funkcja (typ danych)
IS
--zmienne lokalne funkcji

BEGIN
--co chcemy aby funkcja wykonwywala
IF (length(pesel)=11 and length(telefon)=9) then
  RETURN 'TRUE'; --zwracana wartosc przez funkcje
ELSE
  RETURN 'FALSE'; --zwracana wartość przez funkcje
  END IF;
END PESEL; --koniec funkcji


create or replace FUNCTION REJESTRACJA  --tworzenie funkcji o nazwwie FUNCTION1
---MIEJSCE NA ZMIENNE WEJŚCIOWE
(imie in VARCHAR2,
nazwisko in VARCHAR2,
adres in VARCHAR2,
pesel in VARCHAR2,
telefon in VARCHAR2,
login in VARCHAR2,
haslo in VARCHAR2,
phaslo in VARCHAR2)
RETURN VARCHAR2 --co zwraca funkcja (typ danych)
IS
--zmienne lokalne funkcji
ilosc NUMBER;
BEGIN
--co chcemy aby funkcja wykonwywala
IF (length(imie)>0 and length(nazwisko)>0 and length(adres)>0) and length(pesel)>0 and length(telefon)>0 and length(login)>0 and length(haslo)>0 and length(phaslo)>0 then
  RETURN 'TRUE'; --zwracana wartosc przez funkcje
ELSE
  RETURN 'FALSE'; --zwracana wartość przez funkcje
  END IF;
END REJESTRACJA; --koniec funkcji


create or replace FUNCTION UZYTKOWNIKDANE  --tworzenie funkcji o nazwwie FUNCTION1
---MIEJSCE NA ZMIENNE WEJŚCIOWE
(imie in VARCHAR2,
nazwisko in VARCHAR2,
adres in VARCHAR2,
pesel in VARCHAR2,
telefon in VARCHAR2)
RETURN VARCHAR2 --co zwraca funkcja (typ danych)
IS
--zmienne lokalne funkcji
ilosc NUMBER;
BEGIN
--co chcemy aby funkcja wykonwywala
IF (length(imie)>0 and length(nazwisko)>0 and length(adres)>0) and length(pesel)>0 and length(telefon)>0 then
  RETURN 'TRUE'; --zwracana wartosc przez funkcje
ELSE
  RETURN 'FALSE'; --zwracana wartość przez funkcje
  END IF;
END UZYTKOWNIKDANE; --koniec funkcji

create or replace FUNCTION UZYTKOWNIKLOGOWANIE  --tworzenie funkcji o nazwwie FUNCTION1
---MIEJSCE NA ZMIENNE WEJŚCIOWE
(login in VARCHAR2,
haslo in VARCHAR2,
phaslo in VARCHAR2)
RETURN VARCHAR2 --co zwraca funkcja (typ danych)
IS
--zmienne lokalne funkcji
ilosc NUMBER;
BEGIN
--co chcemy aby funkcja wykonwywala
IF (length(login)>0 and length(haslo)>0 and length(phaslo)>0) then
  RETURN 'TRUE'; --zwracana wartosc przez funkcje
ELSE
  RETURN 'FALSE'; --zwracana wartość przez funkcje
  END IF;
END UZYTKOWNIKLOGOWANIE; --koniec funkcji