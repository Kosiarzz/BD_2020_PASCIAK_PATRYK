create or replace PROCEDURE INSERT_REJESTRACJA_LOGIN
(loginn IN KLIENT.IMIE%TYPE,
hasloo IN KLIENT.NAZWISKO%TYPE)
AS 
BEGIN
  INSERT INTO KLIENCI_LOGOWANIE (LOGIN,HASLO)
  VALUES (loginn,hasloo);
  COMMIT;
END INSERT_REJESTRACJA_LOGIN;